# Repository instructions

  - Run `air format .` before every commit that changes R source or
    tests.
  - Review and stage the formatter output together with the related
    change.
  - Run `git diff --check` and the relevant tests before committing.
