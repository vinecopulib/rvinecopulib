% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vinecop_getters.R
\name{vinecop_getters}
\alias{vinecop_getters}
\alias{get_matrix}
\alias{get_pair_copula}
\alias{get_all_pair_copulas}
\alias{get_parameters}
\alias{get_all_parameters}
\alias{get_kendall_tau}
\alias{get_all_kendall_taus}
\alias{get_pair_copula}
\alias{get_parameters}
\alias{get_tau}
\alias{get_family}
\alias{get_all_pair_copulas}
\alias{get_all_parameters}
\alias{get_all_kendall_taus}
\alias{get_all_families}
\title{Extracts components of the vinecop object}
\usage{
get_matrix(object)

get_pair_copula(object, tree, edge)

get_parameters(object, tree, edge)

get_tau(object, tree, edge)

get_family(object, tree, edge)

get_all_pair_copulas(object, trees = NA)

get_all_parameters(object, trees = NA)

get_all_kendall_taus(object, trees = NA)

get_all_families(object, trees = NA)
}
\arguments{
\item{object}{a \code{vinecop} or \code{vine} object.}

\item{tree}{tree index.}

\item{edge}{edge index.}

\item{trees}{the trees to extract from \code{object} (\code{trees = NA} extracts all
trees).}
}
\value{
The structure matrix, or pair-copulas, their parameters, Kendall's taus,
or families.
}
\description{
Extracts either the structure matrix, or pair-copulas,
their parameters, Kendall's taus, or families
}
\details{
The \link{get_matrix} method extracts the structure matrix (see
\link{check_rvine_matrix} for more details).

The other \code{get_xyz} methods return the entries corresponding to the
pair-copula indexed by its \code{tree} and \code{edge}: \cr

\link{get_pair_copula} = the pair-copula itself (see \link{bicop}).\cr
\link{get_parameters} = the parameters of the pair-copula (i.e., a \code{numeric}
scalar, vector, or matrix).\cr
\link{get_family} = a character for the family (see \link{bicop} for implemented families).\cr
\link{get_kendall_tau} = a \code{numeric} scalar with the pair-copula Kendall's tau.\cr

The \code{get_all_xyz} methods return lists of lists, with each element
corresponding to a tree in \code{trees}, and then elements of the sublists
correspond to edges.
}
\examples{
# specify pair-copulas
bicop <- bicop_dist("bb1", 90, c(3, 2))
pcs <- list(
    list(bicop, bicop),  # pair-copulas in first tree 
    list(bicop)          # pair-copulas in second tree 
)

# specify R-vine matrix
mat <- matrix(c(1, 2, 3, 1, 2, 0, 1, 0, 0), 3, 3) 

# set up vine copula model
vc <- vinecop_dist(pcs, mat)

# get the structure matrix
all.equal(get_matrix(vc), mat)

# get pair-copulas
get_pair_copula(vc, 1, 1)
get_all_pair_copulas(vc)
all.equal(get_all_pair_copulas(vc), pcs, check.attributes = FALSE)

}
